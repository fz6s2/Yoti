server:
  port: 8080

spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: "${POSTGRES_URL}"
    username: "${POSTGRES_USER}"
    password: "${POSTGRES_PASSWORD}"

app:
  constraints:
    route-length: 1000
    room-size-x: 1000
    room-size-y: 1000
    patch-count: 1000